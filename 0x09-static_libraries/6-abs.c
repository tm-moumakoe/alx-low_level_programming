#include "main.h"

/**
 * _abs - Computes the absolute value of a given integer.
 * @n: The int to check.
 * Return: The absolute value of the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
